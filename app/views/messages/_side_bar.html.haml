.side-bar
  .side-bar__header
    .side-bar__header__items
      .user
        = current_user.name
        masa
      .icons
        = link_to new_group_path do
          = icon('far','edit')
        = link_to edit_user_path(current_user) do
          = icon('fas','cog', class: 'icon')

  .side-bar__group-list
    - current_user.groups.each do |group|
      .side-bar__group-list__summary
        = link_to '#', class: 'group-list_link' do
          .name
            = group.name
        .latest-message
          %p まだメッセージがありません
      .side-bar__group-list__summary



        .name
          %p techcamp
        .latest-message
          %p まだメッセージがありません

